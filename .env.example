# Production Render : lier la base PostgreSQL (DATABASE_URL est injecté automatiquement)
# Développement local : laisser DATABASE_URL vide, utiliser SQLite
DATABASE_PATH=data/notaria.db
JWT_SECRET=changez-moi-avant-toute-mise-en-ligne
