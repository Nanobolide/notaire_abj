# === DÉVELOPPEMENT LOCAL (SQLite) ===
# Ne pas définir DATABASE_URL — la base SQLite est utilisée automatiquement.
DATABASE_PATH=data/notaria.db
JWT_SECRET=dev-secret-changez-moi

# === PRODUCTION RENDER (PostgreSQL) ===
# DATABASE_URL est injecté automatiquement par Render — ne pas le mettre en local.
# JWT_SECRET=...
